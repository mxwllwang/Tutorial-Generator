# Don't track content of these folders
*.pyo
*.pyc
sample-files/
venv/
instance/
tutorial-generator/instance

# tutorial-generator/flaskr/uploads has a separate 
# .gitignore inside it to keep it as an empty directory